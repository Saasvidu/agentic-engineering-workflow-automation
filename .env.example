# ============================================================================
# Docker Compose Environment Configuration
# ============================================================================
# Copy this file to .env and fill in the values
# cp .env.example .env

# ============================================================================
# PostgreSQL Database Configuration
# ============================================================================
POSTGRES_USER=fea_user
POSTGRES_PASSWORD=fea_password
POSTGRES_DB=fea_db
POSTGRES_PORT=5432

# ============================================================================
# MCP Server Configuration
# ============================================================================
# Database connection URL (automatically constructed from POSTGRES_* vars above)
# Format: postgresql://user:password@host:port/database
# For Docker Compose, use: postgresql://fea_user:fea_password@postgres:5432/fea_db
DATABASE_URL=postgresql://fea_user:fea_password@postgres:5432/fea_db
MCP_SERVER_PORT=8000

# ============================================================================
# Orchestrator Service Configuration
# ============================================================================
# OpenAI API Key for LLM access (required)
OPENAI_API_KEY=your_openai_api_key_here

# MCP Server URL (for Docker Compose, use service name)
# For local development: http://localhost:8000
# For Docker Compose: http://mcp-server:8000
MCP_SERVER_URL=http://mcp-server:8000
ORCHESTRATOR_PORT=8501

# ============================================================================
# FEA Worker Configuration
# ============================================================================
# MCP Server API Base URL (for Docker Compose, use service name)
# For local development: http://localhost:8000
# For Docker Compose: http://mcp-server:8000
API_BASE_URL=http://mcp-server:8000

# Abaqus command path (full path to abaqus.bat or abaqus.exe)
# Example Windows: C:/SIMULIA/Commands/abaqus.bat
# Example Linux: /usr/bin/abaqus
ABAQUS_CMD_PATH=

# Poll interval in seconds (how often worker checks for new jobs)
POLL_INTERVAL_SECONDS=5

# ============================================================================
# Legacy/Deprecated Variables (for backward compatibility)
# ============================================================================
# These may be used by old scripts but are not required for Docker Compose
GOOGLE_API_KEY=
